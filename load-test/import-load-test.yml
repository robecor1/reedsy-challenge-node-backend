config:
  target: "http://localhost:3000"
  phases:
    - name: "Load test"
      duration: 20
      arrivalRate: 100
scenarios:
  - name: Post imports
    flow:
      - post:
          url: "/import"
          json:
            bookId: "1"
            type: "epub"
  - name: Get imports
    flow:
      - get:
          url: "/import"