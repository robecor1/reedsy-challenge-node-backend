config:
  target: "http://localhost:3000"
  phases:
    - name: "POST Load test"
      duration: 10
      arrivalRate: 100
scenarios:
  - name: Get exports
    flow:
      - get:
          url: "/export"
  - name: Get imports
    flow:
      - get:
          url: "/import"