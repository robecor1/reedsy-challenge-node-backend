export enum JOB_STATE {
  PENDING = 'pending',
  FINISHED = 'finished'
}

export enum JOB_TYPE {
  IMPORT = 'import',
  EXPORT = 'export'
}

export enum BOOK_TYPE {
  EPUB = 'epub',
  PDF = 'pdf',
  WORD = 'word',
  WATTPAD = 'wattpad',
  EVERNOTE = 'evernote',
}
